# 경영관리 시스템 — 1차 오픈 8화면

개발 사양서 v1.0 (2026-08-27) 1차 범위의 구현입니다. 사양서와 어긋난 곳은 [erp-spec-gaps.md](./erp-spec-gaps.md)에
전부 적어 두었습니다.

## 접근

`/erp` — 로그인 필요, `noindex`. 첫 진입 화면은 **현금흐름표**입니다.

## 화면 (8)

| 묶음             | 화면                             | 근거                                   |
| ---------------- | -------------------------------- | -------------------------------------- |
| 핵심 원장 (원본) | 집행원장 · 지출 원장 · 수입 원장 | §6.2 · 필터 10종 · 코드 클릭 상세      |
| 핵심 원장 (원본) | 계정과목 체계                    | §8.1 마스터 · 자동 판정 3종            |
| 지금 쓰는 것     | 현금흐름표                       | §9.1 · 일 · 월 · 연 · 블록별 승인 패널 |
| 지금 쓰는 것     | 현금 현황                        | §9.2 · 부족액 3종 · 우선순위 override  |
| 지금 쓰는 것     | 승인 대기                        | 일괄 검토 · 자금 소요 계산             |
| 지금 쓰는 것     | 종합 현황                        | 확정도 배지 · 이관 검증 리포트(§5.5)   |

`⌘K` / `Ctrl+K`로 화면 이동, 상단 검색은 현재 화면의 표에 걸립니다. 어느 화면에서든 집행원장 코드를 누르면
적요 원문 · 3축 · 이력 · 전표 · 승인/반려/보류/취소 · 우선순위 상향이 한 드로어에서 끝납니다.

## 코드 배치

```
shared/erp/            도메인 코어 — 서버·클라이언트 공용, 부수효과 없음
  types.ts             §6.2 entry 외 타입
  accounts.ts          §8.1 마스터 + 판정 3종 (우선순위 · CF 구간 · 운영비)
  codes.ts             §7.1 EX-YYMMDD-NN · -R1 · -C
  status.ts            §7.2 상태 머신 · §7.3 상태별 지표 반영
  cashflow.ts          §9.1 현금흐름표 · 미확정 승계
  cashPosition.ts      §9.2 부족액 3종 · 판정 대기 토글
  permissions.ts       §13 역할 매트릭스 · 승인 금액 구간 · 급여 마스킹
  duplicates.ts        §13.2 중복 탐지 (같은 거래처 · 금액 · 7일)
  migrationChecks.ts   §5.5 V1~V8
  seed.ts              §5.4 시드 27건 + 일계 7행 (정답지)

server/erp/
  store.ts             원장 저장소 인터페이스 + 메모리 구현
  drizzleStore.ts      MySQL 구현
  service.ts           §7 전이 · §10 계약 · §13 통제 · 전표 자동 생성
  router.ts            §10.1 엔드포인트 (tRPC)
  errors.ts            §10.3 오류 — 메시지가 사용자에게 그대로 노출됨
  acceptance.test.ts   §18 T1~T15

drizzle/erpSchema.ts   §6 테이블 (금액은 전부 BIGINT 정수 원)
client/src/pages/erp/  8화면
```

## 지켜지는 규칙

- **파생 뷰를 저장하지 않습니다.** 현금흐름표·현금현황 전용 테이블이 없고 매번 원장에서 계산합니다 (§4).
  그래서 두 화면의 숫자가 어긋날 수 없습니다 (G6).
- **물리 삭제가 없습니다.** DELETE를 실행하는 코드가 없고, 취소는 `-C` 상계 전표로만 합니다 (원칙 9).
- **모르면 null입니다.** 0으로 메우지 않고 `null_reason`과 무엇이 필요한지를 함께 돌려줍니다 (§10.2 · 원칙 8).
- **승인 없이는 지표가 아닙니다.** 승인 대기는 예약런웨이에만 반영됩니다 (§7.3).
- **개인별 급여는 응답 단계에서 마스킹합니다.** 프론트에서 숨기지 않습니다 (§13.3).

## 실행

```bash
pnpm install
pnpm dev          # http://localhost:3000/erp
pnpm test         # §18 T1~T15
pnpm check        # 타입 검사
```

### 환경변수

| 이름               | 없을 때                                  | 용도                                                                         |
| ------------------ | ---------------------------------------- | ---------------------------------------------------------------------------- |
| `DATABASE_URL`     | §5.4 시드로 채운 메모리 저장소로 동작    | MySQL 접속                                                                   |
| `ERP_ROLE_MAP`     | —                                        | `{"이메일":"역할"}` JSON. 역할은 대표·부대표·재무·사업부리더·담당자·외부세무 |
| `ERP_DEFAULT_ROLE` | 개발 환경은 `재무`, 프로덕션은 접근 차단 | 배정 없는 사용자의 역할                                                      |

`DATABASE_URL`이 없으면 시드 27건이 메모리에 올라가고 8화면이 그대로 동작합니다 —
승인·수정·취소도 되지만 프로세스가 재시작되면 초기화됩니다. 실사용 전에 DB를 붙이십시오.

### DB 적재

```bash
pnpm db:push      # drizzle/schema.ts + drizzle/erpSchema.ts 마이그레이션
```

계정과목 마스터가 비어 있으면 §8.1 초기 적재분을 코드에서 읽어 씁니다. 원장 시드 이관은
`shared/erp/seed.ts`의 `SEED_ENTRIES` · `SEED_DAY_SNAPSHOTS`를 그대로 INSERT하면 되고,
적재 후 종합 현황 화면의 이관 검증 리포트로 V1~V8을 확인합니다 (G2 · G3).

## 2 · 3차

채권 · 부채 · 13주 자금계획 · 전표·분개장 · 손익 · 재무제표 5종은 1차가 실사용에 들어간 뒤 붙입니다.
스키마와 판정 함수는 이미 3차까지의 축(계정과목 · 원가성격 · 귀속 · CF 구간 · 운영비 포함 여부)을 담고 있습니다.
